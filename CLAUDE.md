# Claude/CodeCode bootstrap

Before acting, read and obey `AGENTS.md`. Then read `OWNERS.md`, `STATE.md`,
`TASKS.json`, and `LOG.md` from the canonical integration ref specified there.
Do not rely on conversation history as project authority.
