# WARDROBE Web 2.0 — HANDOFF

Складено 2026-07-29 для передачі новому агенту/сесії. Усе нижче — зі стану на диску й у
GitHub на момент запису, не з пам'яті.

---

## 1. ЩО ЦЕ

Кінематографічний scroll-driven односторінковий сайт: камера на рейках проїжджає квартирою,
на дзеркалах живе інтерфейс вибору образу, у фіналі — наїзд на екран ноутбука з живою сторінкою
пайплайну. Продукт нічого не продає: немає цін, кошика, «замовити» — дієслова це згенерувати,
приміряти, подивитись.

Два сеанси Claude Code працювали над ним паралельно ввесь час:
- **транспорт і медіа** (цей сеанс) — `engine.js`, `loader.js`, `audio.js`, `gate.js`,
  `b/assets`, `b/motion.json`, конвертація майстрів
- **інтерфейс** (сусідній сеанс, назва `+3 (fork)`) — `ui.js`, `style.css`, розмітка панелей,
  `FUNCTION-MAP.md`

Канон рішень власника (Едвіна) — `LEVEL-DESIGN.md`. Це джерело правди для структури, не опис.

---

## 1.5. ТЕРМІНИ ПРОСТО (щоб не гадати з коду)

- **«Нога» (leg)** — одна з чотирьох сцен-кімнат фільму, які йдуть одна за одною. Нога 0 —
  перша сцена: камера проїжджає повз порожні рейли, тоді рейл з одягом, і закінчується на
  двох дзеркалах.
- **«Станція»** — місце, де скрол зупиняється і з'являється інтерфейс.
- **Дірка, яку треба закрити:** рушій зараз уміє РІВНО ОДНУ станцію на ногу — в самому кінці.
  За каноном нога 0 має мати ТРИ станції по дорозі: порожні рейли (вибрати себе) → рейл з
  одягом (вибрати речі) → дзеркала (образ і дії). Мова й код цих трьох панелей уже готові в
  `ui.js`, але фізично всі три зараз висять в одній і тій самій точці — на дзеркалах у кінці
  кімнати, — бо рушію нема куди їх розвести. Це найважливіший з відкритих технічних боргів,
  бачити п.7 нижче.

---

## 2. ПОСИЛАННЯ

- **Код:** github.com/edwin0912-00/wardrobe-web2 (приватний), гілка `main`, останній коміт
  `1ee39d9`. Створено 2026-07-28, до цього був лише один старий коміт без remote.
- **Апдейт для інших агентів у цьому репо:** [issue #1](https://github.com/edwin0912-00/wardrobe-web2/issues/1)
- **Суміжний, НЕ той самий проєкт:** github.com/edwin0912-00/zeely-ai-engineering-test —
  окремий, формально керований мультиагентний рій (`AGENTS.md`/`OWNERS.md`, оркестратор
  `codex-main`, задачі `WARD-001`–`WARD-006` саме про той самий концепт «шафа з дзеркалами →
  TV → ноутбук»). **Я в ньому не зареєстрований агент — не маю `agent-id`, не маю ліхи, нічого
  туди не пушив.** Лишив лише інформаційний [issue #44](https://github.com/edwin0912-00/zeely-ai-engineering-test/issues/44)
  з посиланням на `wardrobe-web2`. Живий продукт цього рою — `beta.madeforthisjob.com`,
  реальний бекенд, платна генерація — це геть інший артефакт, не плутати з цим прототипом.
- **Локальні прев'ю-сервери** (можуть бути вже мертві після рестарту): `serve.py` на портах
  4190–4193, 4311 — Range-сервер для скрабу відео. Запуск: `PORT=<n> python3 serve.py` з
  кореня репо.
- **Медіа поза git** (виключено `.gitignore`): `~/wardrobe-media-local/` — леджер генерацій
  (`00_GEN_LEDGER.md`), previz, інвентарі, скачані відео.

---

## 3. СТАН НА ДИСКУ ПРЯМО ЗАРАЗ

```
git status --short
?? handoff-2026-07-28/     <- НЕ моє, інша сесія під час рескиду ліміту, не в git
?? mobile/                 <- сеанс opencloud-20260727-bc27e6. СУПЕРЕЧИТЬ канону в ТРЬОХ
                               місцях: MOTION як крок, SIGNATURE платний блок, мова «замовлено».
                               Це НЕ "доробити файл" — фундамент скасовано, писати з нуля.
```

Усе інше закомічено. `.gitignore` виключає `*/assets/`, `*/audio/` (бінарники генерації),
`a/` (архівна версія A), `*.bak*` (локальні safety-копії).

---

## 4. ЩО ПРАЦЮЄ (зміряно, не заявлено)

- **Плівка:** інтро 8.05с + 4 сцени, усі 1080p/60fps/8-біт/нуль B-кадрів/faststart/без аудіо.
  60fps зроблено локально безкоштовно: інтерполяція з компенсацією руху → усереднення пар
  (=180° затвор) → прорідження. Оригінали 24fps у `b/assets/orig-24fps/`.
- **Годинник:** один скролл-скаляр, інерція, скрол→час по зміряній таблиці (кореляція з
  sin(πu) = 1.0000). `b/motion.json` затиснуто під реальні тривалості файлів після 60fps
  переконвертації (бекап `b/motion.json.bak`).
- **Завантаження:** гейт на 3 файлах (трек + інтро + перша кімната) = ~22МБ, не 66МБ. Мінімум
  показу лоадера 1с. Рушій не входить у кімнату, чиє відео не приїхало — тримає на шві зі
  станом `data-gate="loading"`.
- **Звук:** старт вимкнений, заходить на ~19% завантаження. Реверб фіксовано 15%, ехо росте
  по синусоїді до 80% зі швидкістю скролу й падає в нуль у спокої.
- **Геометрія кадру:** правило «спершу ширина» — обидва дзеркала видні на 100% при будь-якій
  формі вікна (перевірено на 10 формах). Ціна — фонові смуги на екранах вищих за 16:9.
  **Замінено правило «покрити сцену»**, яке губило до 29% ширини дзеркала на не-16:9 вікнах.
- **Гейти й переходи:** `canAdvance(leg)` блокує рух без плашки на екрані (стан `held`,
  без CSS — блокування живе в рампі опору). `advanceTo(leg)`/`releaseAndAdvance()` — програмний
  перехід тим самим інерційним ходом; скасовується РЕАЛЬНИМ вводом (`wheel`/`touch`/стрілки),
  не здогадом по позиції скролу — перша версія на здогаді давала фолс-позитиви. Станція з
  гістерезисом: вхід 0.90, вихід 0.72.
- **HTML на екрані ноутбука (фінал):** геометрія зведена до ОДНОГО числа на кадр (півширина
  екрана) — камера йде строго по осі, `matrix3d` не знадобився. Заміряно з точністю 0.17%
  висоти кадру. `mix-blend-mode: screen`, білий 254. Контент поки заглушка — сусідня сесія
  має віддати реальний фрагмент пайплайну.
- **Зерно:** варіант 5 з шести (дрібне срібло + тепле гало), `soft-light`. Числа в
  `:root { --grain-base, --grain-move, --grain-scale }`.
- **UI-концепти (`ui-concepts/index.html`):** п'ять розкладок інтерфейсу на РЕАЛЬНИХ
  зміряних прямокутниках дзеркал, п'ять трактувань блоку дій — макет для узгодження, сам
  по собі не є джерелом для робочого коду і нічого від нього не чекає. Власник обрав
  розкладку 3 і трактування v2; перемикання адресою `?c=3&s=look&v=2`.
  **Канон УЖЕ перенесено в `ui.js` — коміт `1ee39d9`, не чекає.** Перевірка одним grep:
  `ВИ`/`РЕЧІ`/`ОБРАЗИ`/`Фешн-відео`/`OMNI 3`/`Приміряти` — 8 збігів. Мова продажу прибрана
  повністю: ні «замовити», ні цін, ні кошика ніде в `ui.js`/`style.css`/`b/index.html`
  (зміряно grep, нуль збігів). Панелі без рамок — маска-фейд у дзеркалі, кегль у `cqh` з
  підлогою в px (не нижче 9px), нуль переповнення на 5 форм вікна від 842×474 до 1920×1080,
  усі п'ять дій видні завжди. `canAdvance(leg)` — **лише нога 0** (`leg === 0 ? looks.length > 0
  : true`), свідомо: після канону телевізор — галерея, не крок, порожня полиця там не помилка.
  `travelled(how)` приймає вердикт очікування в `state().travel`, три значення перевірені.
  `FUNCTION-MAP.md` теж переписаний під канон — 4 кімнати, не 6 (галерея й відсутність
  продажу закрили дві самі).

  **Не зроблено, наступне в черзі сусідньої сесії:** LIVE-стан — кнопка є, вікна камери
  немає; кнопка «назад» з галереї до вибору; прямокутники телевізора/ноутбука/монітора не
  зміряні (дзеркала — єдина точна поверхня, без інших вимірів класти образ нема куди з тією
  ж точністю); **три станції всередині ноги 0 фізично не існують** — усі три панелі досі
  висять на одній зупинці біля дзеркал, чекають масив станцій із п.6/7 нижче. Це найважливіший
  пункт: канон описаний і закодований у мові UI, але географічно ще не розведений по кімнаті.

---

## 5. ЩО ВІДКРИТО — ЧЕКАЄ РІШЕННЯ ВЛАСНИКА

| # | питання | стан |
|---|---|---|
| 1 | **Перша сцена йде на перегенерацію.** Три варіанти (`seg1-A/B/C.mp4` у `b/assets`) НЕ підходять як фінал — усі три доїжджають до голої стіни/дверей/пілона, а не до двох дзеркал, бо я помилився в промті («завершується на стіні» замість «на дзеркалах»). Матеріал (меблі, хореографія) хороший, кінцівка ні. Previz і зміряна палітра готові в `~/wardrobe-media-local/SCENE1_INVENTORY.md` і `previz-scene1.png` | не перегенеровано |
| 2 | **Шість концептів очікування** готові й забрані в `~/wardrobe-media-local/wait-concepts/`: window, coffee, room-breathes, mirror-thinks, corridor, **sun/golden-hour** (щойно додано). POV, камера = очі глядача, лок «людей нема» не порушено. Sun-варіант монотонний (ранок→golden hour→ніч) — можна грати вперед-назад як луп без другого кліпу | вибір не зроблено |
| 3 | Напис «MacBook Pro» на четвертій сцені (домальована пляма зі стартового кадру) — власник сказав **не чіпати** | закрито, не чіпати |
| 4 | Мобільна версія — **явно віддана окремому чату**, ПІСЛЯ що горизонт запрацює: спершу каркас пайплайну → UI → тоді вертикаль і ПЕРЕГЕНЕРАЦІЯ всіх відео, не переврізка | не починати тут |
| 5 | Бекенду немає. `gate.js` — заглушка, 2.6с таймер, каже про це прямо на екрані | відомий факт, не борг |

---

## 6. РІВЕНЬ-ДИЗАЙН (канон, `LEVEL-DESIGN.md`)

Станцій у нозі 0 тепер **три**, не одна: порожні рейли (обрати персонажа, до двох фото) →
рейл з одягом (обрати речі) → два дзеркала (образ і дії). Телевізор — **не крок, а галерея**
готових фотозйомок і відео. На дзеркалах: ліве питає, праве показує; дії з'являються лише
коли образ уже видно — змінити фон (→ відео в OMNI 3, окрема гілка, не прописана), фотозйомка
в стилі (поруч, не після — відео у фешн-стилі), лайв-примірка.

**Наслідок для рушія:** потрібен масив станцій на ногу з окремим гістерезисом і гейтом на
кожну, замість одного `stationAt`. Ще не написано.

---

## 7. ПОРЯДОК, ЯКИЙ Я Б ТРИМАВ

1. Вибір власника: концепт очікування (п.5.2) і рішення по першій сцені (п.5.1)
2. Масив станцій у `engine.js` під нову географію нульової ноги
3. Перегенерація сцени 1 з правильним фіналом (на дзеркалах, не на стіні)
4. Три кліпи очікування за обраним концептом (відліт/пауза + повернення, або луп сонця вперед-назад)
5. Перенесення мови UI-концептів у робочий `ui.js` (сусідня сесія)
6. Реальний фрагмент пайплайну на екран ноутбука (заміна заглушки)
7. Бекенд — після того, як кадр і кімнати замкнені

---

## 8. ПОПЕРЕДЖЕННЯ, ЯКІ КОШТУВАЛИ ЧАСУ ЦЬОГО СЕАНСУ

- **MCP-пулінг Higgsfield через `--wait-timeout` — неробочий флаг.** `generate wait` його не
  приймає; хибний скрипт годину звітував «in progress», хоча роботи вже давно завершились.
  Перевіряти статус через `generate list --json`, не `generate wait`.
  Ідентична пастка вже записана в проєктну пам'ять (feedback-файли), але я сам у неї вступив.
- **Higgsfield/Magnific — тільки через MCP**, не CLI/API (правило зʼявилося посеред сесії;
  три перші генерації сцени 1 пішли через CLI до того, як я це прочитав — не скасовувати
  заднім числом, але далі тільки MCP).
- **`--end-image` у Seedance замерзає в хвості** — до 109 мертвих кадрів. Для точного
  повернення в кадр краще ланцюжок «попередній останній кадр = наступний перший», не
  end-image.
- Три бекапи (`ui.js.bak-precanon`, `style.css.bak-precanon`, `b/motion.json.bak`) лежать
  на диску, у git не йдуть за правилом `.gitignore` — не видаляти, це safety-копії за
  протоколом `safe-artifact-stewardship`.
