# Incident c8780403-9adf-43d7-a600-653b751b8a75

Upload and immutable run creation succeeded. `GARMENT_CONDITIONING` classified two views of the same blue/white pinstriped shirt as two separate `top` garments and stopped with `DUPLICATE_SLOT`. Expected: group strongly verified views of the same exact physical garment into one reference set, condition it once using all views, retain full provenance, and keep truly distinct same-category garments fail-closed.
