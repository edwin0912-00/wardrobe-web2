# Evaluator verification loop

`loop.yaml` is the only hand-maintained loop specification. The resolved JSON,
rendered description and in-session handoff are generated by Looper.

The loop is intentionally programmatic-only: it changes no production state,
sends no repository material to an additional model, and finishes only when
the root evaluator command, local product journey and deployed parity checks
all pass.
