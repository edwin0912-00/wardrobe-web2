# WARDROBE Web 2.0 — карта стану

Складено 2026-07-28 з перевірених фактів, не з пам'яті. Версія B — єдина в роботі; A архівна.
Два сеанси працюють паралельно: **транспорт і медіа** (engine.js, loader.js, audio.js, gate.js,
b/assets, motion.json, конвертація майстрів) і **інтерфейс** (ui.js, style.css, розмітка панелей).

---

## 1. ЩО ЖИВЕ І ЗМІРЯНО

**Плівка.** Інтро 8.05 с + чотири сцени, усі 1080p / 60 к/с / yuv420p / нуль B-кадрів /
faststart / без аудіо. Кейфрейм ≤250 мс. Дублікатів кадрів нуль. Разом 66 МБ.
60 к/с зі справжнім 180° затвором зроблені локально: 24 → інтерполяція з компенсацією руху до
120 → усереднення пар → прорідження до 60. Оригінали 24 к/с у `b/assets/orig-24fps/`.

**Годинник.** Один скалярний скрол, інерція, скрол→час по зміряній таблиці (кореляція з
sin(πu) = 1.0000, пік на 0.500). Шов між сценами — короткий, бо кінець ноги N і початок N+1
різняться на 2.5% пікселів.

**Завантаження.** Гейт на трьох файлах (трек + інтро + перша кімната) = 22 МБ, не 66. Відсоток
сирий, без клампа. Мінімальний показ 1 с, щоб не блимав на теплому кеші. Решта фільму
довантажується під запущеним скролом; рушій не входить у кімнату, чиє відео не приїхало —
тримає на шві зі станом `data-gate="loading"` і каже чому.

**Звук.** Стартує вимкненим, заходить на 19% завантаження. Нуль накладань між треками.
Кнопка каже те, що чути, а не «відтворення почалося».

**Геометрія кадру.** Правило «спершу ширина»: обидва дзеркала видні на 100% при кожній формі
вікна (перевірено на 10 формах). Ціна — фонові смуги на екранах вищих за 16:9: 0 px на
1920×1080, 121 px на 1280×1024.

**Гейти й переходи.** `canAdvance(leg)` блокує рух уперед без плашки на екрані (стан `held`).
`advanceTo(leg)` / `releaseAndAdvance()` — програмний перехід тим самим інерційним ходом;
скасовується справжнім вводом, не здогадом по позиції. Станція з гістерезисом: вхід 0.90,
вихід 0.72.

**Зерно.** Варіант 5 — дрібне срібло + гало, `soft-light`. Один власник, числа в `:root`.

**Інтерфейс (сусідній сеанс).** Чотири кроки: ВИ → LOOK → ФОН → MOTION. Аватар перший, LOOK —
п'ять слотів і одна кнопка. Ліве дзеркало питає, праве показує.

---

## 2. ВІДКРИТЕ, ЧЕКАЄ РІШЕННЯ ВЛАСНИКА

| # | питання | ціна рішення |
|---|---|---|
| 2.1 | **Напис «MacBook Pro» на четвертій сцені.** Ретейк не полікував: причина — світла пляма 14×6 px на рамці у стартовому кадрі, яку модель домальовує у вордмарк. Варіанти: почистити пікселі у стартовому кадрі й перегенерувати · замалювати в доставці · лишити | 330 кр або домальовка |
| 2.2 | **Концепт очікування.** Подано п'ять. Після уточнення POV актуальні: вікно (ваш, з хендхелдом), кімната дихає, коридор | 2–3 × 330 кр |
| 2.3 | **Три варіанти першої сцени** — генеруються, вибір за вами | витрачено 990 кр |
| 2.4 | **Числа зерна** — сила й розмір; я поставив дефолти, бо зчитувати з пікселів скріншота не буду | безкоштовно |
| 2.5 | **Музика: у нас два твори, не три.** `t2` і `t3` — два рендери одного, тривалість збігається до шостого знака. На чотири ноги це мало | новий трек |

---

## 3. ЩО МИ ПРОПУСТИЛИ — не обговорювалося жодного разу

### 3.1 FUNCTION-MAP.md не існує
Головна прогалина. Це був гейт-делівербл власного луп-плану: кожна продуктова функція —
LOOK, WORLD, MOTION, SIGNATURE, LIVE LAB, збережені проєкти, чек рендера — має бути посаджена
в конкретну КІМНАТУ на конкретну ПОВЕРХНЮ, з причиною і з вимогою гейта. Зараз частина функцій
живе тільки в описі, і не перевірено, чи для кожної взагалі є поверхня в цих чотирьох кімнатах.
**Без цього ми не знаємо, чи фільм показує весь продукт.**

### 3.2 Мобільний не існує
Уся плівка 16:9. Портретних мастерів немає, і це не переврізка: два дзеркала не влазять у 9:16
у придатному масштабі. На телефоні сайт зараз — це або смуги на пів екрана, або обрізані
дзеркала з інтерфейсом за кадром. Найбільша непокрита територія.

### 3.3 Бекенду немає
`gate.js` — оголошена заглушка: перевіряє наявність файлів, чекає 2.6 с і каже «готово».
Жодної справжньої генерації образу. Уся послідовність LOOK→WORLD→MOTION — мок.

### 3.4 Нічого не задеплоєно
`site.madeforthisjob.com` не чіпали. Сайт живе лише на локальних портах.

### 3.5 Гейти є тільки на першій нозі
`REQUIREMENTS` у gate.js має один запис. Ноги 1–3 проходяться вільно.

### 3.6 Копірайт англійською, інтерфейс українською
Чотири рядки на екрані — англійські, панелі — українські. Ніхто не вирішував, чи так і треба.

### 3.7 Аудіо-атмосфера не прийнята
`ambience.mp3` завалений у низ: 66% енергії нижче 100 Гц, 6% вище 500. Ніколи не перероблялося.

### 3.8 Луп не запускався
`~/wardrobe-loop` спроектований, `RUN_IN_SESSION.md` жодного разу не виконано.

---

## 4. СПЕЦИФІКОВАНО, НЕ ПОБУДОВАНО

### 4.1 HTML-презентація на екрані ноутбука (фінал)
Жива інтерактивна сторінка всього пайплайну, накладена на екран у четвертій сцені.
Вимоги власника: той самий кут нахилу, що в монітора; кей по чорному; біла точка 254;
чорне презентації зливається з екраном.
Технічно: `mix-blend-mode: screen` на шарі поверх відео дає рівно це — чорне пропускає
картинку знизу, світле додається. Кут — CSS `matrix3d`, порахований з чотирьох кутів екрана
на кадрі. Ще не зміряні кути; ще не написано.

### 4.2 Кліпи очікування (після вибору концепту)
Уточнення POV змінює структуру: **три кліпи, не один.**
Відліт (хендхелд, погляд наліво) → луп біля вікна (будь-яка тривалість) → повернення
(хендхелд, погляд направо, кінчається рівно на фронтальному кадрі дзеркал).
Реверс не годиться — на поверненні погляд інший.
Зворотний кліп робити з `--end-image`: його замерзлий хвіст тут не брак, а посадкова смуга —
обрізати по першому мертвому кадру й стоїш рівно на цілі.
Хендхелд ламає рейку свідомо: рейка — камера системи, POV — око людини.

### 4.3 `data-wait` у рушії
Стан і прогрес для інтерфейсу на час роботи бекенду. Не написаний свідомо: залежить від
вибраного концепту, і код під невибраний варіант може виявитись непотрібним.

---

## 5. ДРІБНЕ, ВИПРАВЛЕНО ЩОЙНО

`motion.json` для ноги 0 показував на 13.25 с при файлі 13.233 після обрізки дубльованого
кадру — дві хвостові мітки за межею файлу. Затиснуто по реальній тривалості, бекап
`motion.json.bak`. Криві нормалізовані, тому решта таблиці валідна без перебудови.

---

## 6. ПОРЯДОК, ЯКИЙ Я БИ ТРИМАВ

1. **FUNCTION-MAP.md** — безкоштовно, і без неї не видно, чи фільм показує продукт (3.1)
2. Вибрати варіант першої сцени з трьох, що генеруються
3. Вирішити напис на четвертій сцені (2.1) — це єдиний живий лок-брейк у доставці
4. HTML на екрані (4.1) — фінал фільму, і він безкоштовний
5. Мобільний (3.2) — найбільша непокрита територія, і рішення там продуктове, не технічне
6. Кліпи очікування (4.2) після вибору концепту
7. Бекенд (3.3) — після того, як кадр і кімнати замкнені
