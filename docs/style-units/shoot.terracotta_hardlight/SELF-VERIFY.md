# SELF-VERIFY — shoot.terracotta_hardlight

`frame-01` and all seven git-preserved sheet PNGs were inspected at full available resolution. This
unit follows the corrected single-frame-universe policy: the supplied frame anchors the warm,
hard-light photographic world and the sheets define six original compositions inside it. Those
compositions are labelled `GENERATIVE EXTRAPOLATION`; they are not presented as additional source
frames. Source identity and source garment design are excluded.

## Seven-sheet review

- `camera_lens` — PASS as production grammar. It preserves moderate non-wide perspective, clean
  optics, legible hard shadow geometry and controlled focus falloff; alternate camera positions are
  new-shot constraints rather than source measurements.
- `blocking` — PASS. Body-away/head-return tension is the seed; the completed mannequin chains are
  distinct reusable pose designs, not claims about cropped feet, support leg or hidden hands.
- `expression_gaze` — PASS for direct or near-direct unsmiling muscle state, low lids, resting
  brows and cheekbones, relaxed jaw and level mouth corners. Generic faces are not identity assets.
- `garment_behaviour` — PASS. One moving approved-cloth plane, crisp edge, light-dependent
  transmission and weight-aware transfer are reusable; the source garment's cut and colour do not
  transfer, and runtime leaves the physical motion cause unspecified.
- `colour_grade` — PASS. `palette-strip.svg` is sole authority for the exact five-value warm
  palette; actual skin/hair/eyes and approved-item colours remain exempt.
- `environment` — PASS as original set design: one matte terracotta-colour plane, clear air and one
  straight diagonal light/shadow cut. No real wall, room, caster, landmark or prop is copied.
- `person` — PASS as an approved-subject inspection and hard-light response template only. It
  supplies no face, anatomy, marks, complexion, age, gender or ethnicity.

## Six-shot coverage review

- `clean_identity_hero` — PASS: chest-height back-three-quarter portrait with head returned to lens
  and no cloth over the face.
- `environmental_hero` — PASS: level wider frame gives the subject and full diagonal field distinct
  visual weight.
- `sculptural_three_quarter` — PASS: low oblique view opens the approved item as one asymmetric
  curved plane.
- `interference_frame` — PASS: a soft near approved edge crosses one side while the returned eyes
  remain sharp.
- `material_or_accessory_detail` — PASS: hand, wrist, torso and approved construction remain
  connected in a hard-rim detail.
- `wide_campaign_coda` — PASS: horizontal oblique wall view retains a subject at or above thirty
  percent of frame height and introduces no furniture or unrelated depth.

## Palette, optics, identity and preservation gates

The Master Gamma is exactly `#B5543A #5A2A20 #F2EDE4 #C89275 #B8552F`. Every frame keeps
clean moderate optics, fine low grain, hard set-plane geometry, smooth bright-edge roll-off and only
slight halation on the brightest approved-cloth edge. Flare, vignette, global bloom, haze, chromatic
fringe and swirl are forbidden.

Every current PNG matches legacy manifest SHA-256
`ac491c6b7cfae8047fefda3b94251e8bcb84d0951627281316f62021a2c290d2`. The original set is rooted in
commit `c8a4c1b0d33392e9205423432f4d94823bdbe0fb`; six restored full-resolution sheets are additionally
preserved by `05778462a69de508fdc83d4a4450357588c0f8b3`. Provider job identifiers were not retained:
`provider_receipt` remains null and each sheet uses a truthful `legacy_artifact_receipt`.

UNIT VERDICT: APPROVED
