# Chat 3 — Beta Block 3: standard backgrounds

Owner: `chat-3`.
Branch: `beta-block-3-backgrounds`.
Pipeline: `BACKGROUND.01–02`, background-video choice shell.

Own sixteen `std.*` packs, their provenance/hashes, picker and scene result/
retry/save UI. Do not edit scene backend or QA; Block 1 owns those.

First atom: prove all sixteen cards and previews, then drive one saved look
through one selected preset into Block 1 execution/QA and persisted result.
