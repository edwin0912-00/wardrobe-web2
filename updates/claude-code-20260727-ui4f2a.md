# claude-code-20260727-ui4f2a

Agent entry: працюю за прямим завданням Едвіна (AGENTS.md fast rule 3), тому
створюю власні рядки на дошці й публікую звіт до правок продуктового коду.

## BETA-LOOKPANEL-001 — STARTED

PROFILE.03 · панель збереженого образу.

Знайдено три дефекти, усі підтверджені замірами геометрії, не на око:

1. Панель була прозора. `scene-cta.css` свідомо ставив
   `.profile-look-detail{background-color:transparent}`, щоб `mix-blend-mode:
   multiply` на PNG лягав на поверхню під ним. Але це абсолютний оверлей над
   `.profile-look-grid`, тож картки збережених образів читалися крізь відкриту
   панель. Панель прибрана зі списку прозорих; її власний непрозорий тон і є
   тією поверхнею, тому шов multiply лишається закритим. Сітка і сцени-стейджі
   прозорими лишились.
2. Панель прокручувалась і зрізала заголовок: зображення мало `height:100%` з
   `max-height:520px` без `min-height:0`, тому на вікні 800px вміст був 579px
   проти панелі 429px. Тепер зображення стискається, панель `overflow:hidden`, а
   прокручуються лише списки.
3. Ряд кнопок накривав заголовок «Fashion-фотосесії»: висота оверлея диктувалась
   висотою сітки карток. Додано підлогу `min-height: clamp(430px, 58vh, 700px)`,
   обмежену `min-width: 701px`, бо портретна гілка має власну робочу розкладку.

Перевірка: 1280x800, 1366x768, 1440x900, 1920x918 і стрес-кейс на девʼять сцен —
нуль перекриттів між сусідніми блоками, нічого не виходить за низ панелі,
заголовок і всі три кнопки завжди в кадрі.

## BETA-UNIVERSE-PREVIEW-001 — PROPOSED, зупинено на колізії

UNIVERSE.02 · превʼю Art Fashion.

Пʼять `shoot.*` режимів показували технічні контакт-шити, бо
`editorialModePreview` для `create_universe` завжди бере асет за `preview_role`
(`environment` або `blocking`), а в юнітах лежать лише шити.

Джерела знайдені поза репозиторієм — доставлені кадри зйомок. Взяті як є,
cover-кроп до 4:5, **без жодної нової генерації**:

| режим | джерело |
| --- | --- |
| `shoot.skylight_haze` | `002-skylight_haze.png` |
| `shoot.window_gobo_warm` | `001-window_gobo_warm.png` |
| `shoot.sky_dune_surreal` | `001-sky_dune_surreal-V2.png` |
| `shoot.grey_studio_stride` | `001-grey_studio_stride.png` |

Для `shoot.terracotta_hardlight` доставленого кадру не існує, тому взято реальний
студійний знімок тканини з його ж `sheet-garment_behaviour`. Це **не** обхід
блокування терракоти: превʼю не змінює жодного хеша reference-пакета і не
відкриває генерацію. Тип походження записаний у `origin` сайдкара окремо
(`SHEET_PHOTO_PANEL` проти `REAL_SHOOT_FRAME`), щоб їх не сплутали.

Усі пʼять карт перевірені проти контракту превʼю: рівно 1024x1280 webp, один
кадр, `sha256` сходиться, `ui_name_uk` збігається з каталогом. 5/5 OK.

**Стан: ASSETS_ONLY — NOT IN PRODUCT.** Бракує саме інтеграційної поверхні
`src/web/scene-resolvers.js` → `editorialModePreview`: мудкарта має вигравати,
коли існує на диску, інакше шит як раніше. Цей файл зарезервований активним
рядком `BETA-UNIVERSE-001` (antigravity-20260727-fb7a90, READY), тому за fast
rule 3 я не торкаюсь його і зупиняюсь на розведенні колізії. Готовий патч
тримаю поза гілкою; віддам власнику рядка або застосую після переуступки.

## Чого я не робив

- Не перезапускав бету: у процесі був живий прогін користувача.
- Не чіпав `main`, креденшали, `site.madeforthisjob.com` і порт 4180.
- Не пушив нічого, крім рядків дошки, цього звіту й файлів своїх задач.

## BETA-LOOKPANEL-001 — READY_FOR_BETA_DEPLOY

Точний протестований коміт: `6e9cc68`.
Фокусна перевірка: `node --test test/web/profile-ui-flow.test.js
test/web/profile-navigation.test.js test/web/profile-app-integration.test.js`
→ tests 17, pass 17, fail 0.

Прошу активувати саме цей SHA на `https://beta.madeforthisjob.com`.

## BETA-UNIVERSE-PREVIEW-001 — ASSETS_ONLY — NOT IN PRODUCT

Пʼять мудкарт закомічені як асети. Вони ще НЕ у продукті: превʼю віддає шит,
поки `editorialModePreview` не віддасть перевагу мудкарті.

Бракує рівно однієї інтеграційної поверхні:
`src/web/scene-resolvers.js` → `editorialModePreview` — гілка `if (mode.create_universe)`
має спершу перевірити наявність `assets/scene-mood-cards/<modeId>.{webp,json}` і
віддати мудкарту, інакше шит як раніше. Валідацію контракту чіпати не треба:
карти вже проходять її (1024x1280 webp, один кадр, sha256 сходиться,
ui_name_uk збігається) — перевірено 5/5.

Файл зарезервований активним `BETA-UNIVERSE-001` (antigravity-20260727-fb7a90,
READY), тому я його не торкаюсь. Готовий патч віддам власнику рядка або
застосую після переуступки шляху.

## BETA-PICKER-001 — STARTED

Розведення колізії на `src/web/scene-resolvers.js`: рядок BETA-UNIVERSE-001 у стані
`READY`, не `IN_PROGRESS`; fast rule 2 забороняє перекриття саме з `IN_PROGRESS`.
Завдання дав Едвін напряму двічі (fast rule 3). Зміна адитивна — додається лише
перевага мудкарти в `editorialModePreview`; гілка шита, `preview_role` і валідація
контракту не змінюються. Доказ ненанесення шкоди — зелені тести, які цей рядок
резервує. Один коміт, який власник може відкотити.

Друге в цьому ж рядку: підписи вкладок пікера були константами в розмітці
(«5 стандартних сцен», «Art Fashion · 6 кадрів»), тоді як каталог віддає 16
стандартних пресетів і 7 готових Art Fashion напрямів. Числа беруться з даних.

## BETA-PICKER-001 — READY_FOR_BETA_DEPLOY

Точний протестований коміт: `dbc2442`.
`node --test test/web/editorial-preview-api.test.js test/contracts/scene-production-packs.test.js`
→ tests 7, pass 7, fail 0. Обидва файли резервує BETA-UNIVERSE-001, тому вони і є
доказом цілості його поверхні.

Прошу активувати `dbc2442` на `https://beta.madeforthisjob.com` — до активації
превʼю в браузері лишається шитом, бо релізи ріжуться з `beta` окремим циклом.

## Не зроблено свідомо — потрібне рішення оператора

1. **Прибрати затвердження плану.** Едвін просить, щоб натискання на стиль одразу
   запускало генерацію. Це не UI: `BIBLE_PENDING_APPROVAL` +
   `approveProfileEditorialBible` з `expectedSha256` — серверний хеш-гейт, який
   підтверджує саме той план шести кадрів. Автозатвердження знімає підтвердження
   плану користувачем. Не робив без прямого рішення.
2. **Розкладка екрана зйомки.** «Не підходить» допускає кілька читань: злити план
   і панель генерації в один блок, показувати план збоку, або взагалі не
   показувати план окремо, якщо гейт прибирається. Чекаю на уточнення, щоб не
   переробляти двічі.

## Реліз на сайт — виконано

Активовано `release-1380ff7-20260727195843` (коміт `1380ff7`).

- `run-beta-daemon.sh` → `app_root` перемкнуто, попередній збережено як
  `run-beta-daemon.sh.bak-20260727-ui4f2a`; попередня тека релізу ціла, відкат це
  один рядок.
- Тека релізу зібрана через `rsync --link-dest`, тому забрала ~17 МБ, а не 481 МБ.
- Перезапуск: `launchctl kickstart -k gui/<uid>/com.madeforthisjob.beta`, у момент
  перезапуску живих прогонів не було (0 відкритих файлів у `runs/`).

Перевірено через публічний домен:
`https://beta.madeforthisjob.com/` → 200; усі пʼять `shoot.*` превʼю → 200
`image/webp` 1024x1280; помилок у `beta.log` немає.

### ⚠ Диск на хості майже повний

`/System/Volumes/Data` — 100%, вільно ~800 МБ при 13 теках релізів по ~481 МБ.
Наступний реліз, зібраний звичайним копіюванням, впаде або добʼє диск. Я свою
теку зробив жорсткими посиланнями, але ротацію старих релізів має зробити власник
релізу — я чужі теки не видаляю.

## BETA-VISUAL-SMOKE-001 — PASS (з чесною межею охоплення)

Перевірено проти `https://beta.madeforthisjob.com`, реліз
`release-1380ff7-20260727195843`. Код пікера не переписував: дефектів, які можна
відтворити, не знайшов.

### Превʼю Art Fashion — 9/9 PASS

Усі девʼять напрямів віддають `image/webp` рівно `1024x1280`. Жодного
контакт-шита. Візуально підтверджено на монтажі всіх девʼяти прямо з домену.

| напрям | тип | розмір |
| --- | --- | --- |
| `editorial.edwin_novak.organic_contrast` | image/webp | 1024x1280 |
| `editorial.edwin_novak.urban_monochrome` | image/webp | 1024x1280 |
| `editorial.edwin_novak.institutional_modernism` | image/webp | 1024x1280 |
| `editorial.edwin_novak.luminous_blue_white` | image/webp | 1024x1280 |
| `shoot.skylight_haze` | image/webp | 1024x1280 |
| `shoot.terracotta_hardlight` | image/webp | 1024x1280 |
| `shoot.window_gobo_warm` | image/webp | 1024x1280 |
| `shoot.grey_studio_stride` | image/webp | 1024x1280 |
| `shoot.sky_dune_surreal` | image/webp | 1024x1280 |

### Числа — PASS

`/api/scene-presets` віддає **16** стандартних пресетів, `/api/editorial-modes`
віддає **9** напрямів. У розмітці чисел немає; `scene-ui.js` на домені містить
обидві підстановки з даних і хелпер трьох форм множини. Стара «5 стандартних
сцен» більше не зустрічається.

### Артефакти, що доїхали на домен — PASS

- `scene-cta.css`: `.profile-look-detail` більше не в списку прозорих.
- `result.css`: підлога `min-height: clamp(430px, 58vh, 700px)` присутня.
- `index.html`: кеш-версії обох стилів бампнуті, інакше браузер віддавав би старі.
- `editorial-shoot-ui.js`: гілка автопідтвердження присутня, стейдж плану
  примусово приховано.

### Чого я НЕ перевірив кліками — межа охоплення

Панель збереженого образу і потік «клік на стиль → результати» вимагають
авторизованого профілю зі збереженим образом. У браузері перевірки сесії немає,
тому я перевірив віддані артефакти й API, а не клік-через. Це не PASS на
клік-через і я його не заявляю. Щоб закрити повністю, потрібна сесія з
готовим образом.

### Помилок немає

`https://beta.madeforthisjob.com/` → 200, у `beta.log` після перезапуску
жодного `error`/`throw`.

## BETA-OWNERFRAME-001 — превʼю з наданих власником кадрів

Едвін: результат зйомок у превʼю не ставимо. Надіслані 15 файлів у
`~/Desktop/ZEELY_STYLE_SOURCE`; сімка з них — вихідні кадри самих юнітів, і кожен
збігається з `source_frames` свого манифеста дослівно. Це і є ті джерела, яких я
не знайшов у репозиторії раніше.

Кроп зверху (`position: top`), а не по центру: у цих кадрах голова високо, і
центральний кроп її різав.

### `shoot.skylight_haze` — «Скляний дах · теплий серпанок»

Жінка у квітковій органзі, руки підняті, камера різко знизу, скляний дах
засвічений до майже білого. Пелюсткова напівпрозора тканина ловить контурне
світло, передній план перекритий розфокусованою кремовою масою.
Манифест: `floral organza, arms raised, low camera` — збіг повний.
Палітра юніта `#EAF0F2 #EFE0CE #E8B79A #C98A78 #6F7355 #3B4630` — на кадрі
присутні всі шість: блакитно-біле небо, крем, персик, пудрова роза, олива, темна
зелень листя.

### `shoot.terracotta_hardlight` — «Теракота · жорстке сонце»

Поворот на три четверті спиною, біле льняне в руху, одна терракотова стіна, одне
жорстке сонце, різка діагональна тінь. Тканина тримає одну велику вигнуту
площину, а не тріпочеться — рівно те, що записано в `TRANSFER` шита поведінки.
Манифест: `back three-quarter turn, white linen open in motion against a
terracotta wall` — збіг повний.

### `shoot.window_gobo_warm` — «Тепле вікно · gobo-тінь»

Кадр по середину стегна, стійка на три четверті, одна тепла кремово-грейжева
стіна з великою мʼякою прямокутною тінню вікна, темний зібраний прозорий верх.
Тканина в спокої, не в русі.
Манифест: `portrait cropped mid-thigh, three-quarter stance against one warm
cream-greige wall carrying a large soft-edged rectangular window gobo` — збіг
повний, включно з тим, що це саме кроп по стегно.

### `shoot.grey_studio_stride` — «Сіра студія · крок»

Повний профіль у кроці, безшовний середньо-сірий фон, сухий очерет уздовж низу,
матова майже чорна підлога, взуття повністю в кадрі. Довгий обʼєктив,
рівна лінія погляду.
Манифест: `full-length profile mid-stride walking across a seamless mid-grey
studio backdrop, dried reeds along the base` — збіг повний.

### `shoot.sky_dune_surreal` — «Небо й дюна · сюрреалізм»

Фронтальна постать без емоції на чистому піщаному гребені, бліде синє небо з
окремими купчастими хмарами, білі оправи темних окулярів, вʼязана шапка.
Манифест: `full-length dead-frontal standing figure on a clean sand ridge against
a pale blue sky` — збіг повний, включно з окулярами й шапкою.

### Ще дві картки — поза пікером

`shoot.shutter_amber_interior` (помаранчева квіткова сорочка, смуги жалюзі) і
`shoot.ochre_stage_tailoring` (рух на темному фоні, помаранчевий корд) теж
зібрані. Але в каталозі їх немає: `CREATE_UNIVERSE_MODE_META` містить пʼять
режимів, тож ці дві картки лежать готовими й почнуть працювати рівно тоді, коли
власник `BETA-UNIVERSE-001` введе ці юніти в продукт. Це `ASSETS_ONLY` для них.

### Перевірка контракту

Усі сім: рівно 1024x1280, один кадр webp, `sha256` сходиться, `ui_name_uk`
збігається з каталогом. Пʼять каталожних — 5/5 OK.

`origin` кожного сайдкара: `OWNER_SUPPLIED_STYLE_FRAME`, з `source_sha256`
оригіналу й розміром до кропу. Жодної генерації, жодного ретушу.

### Реліз — виконано

`release-abd9afd-20260727202146` активовано. Попередній збережено як
`run-beta-daemon.sh.bak-ownerframe`, попередня тека релізу ціла.
Тека зібрана через `rsync --link-dest`: делта 16 файлів.

Перевірено через публічний домен: усі пʼять превʼю `image/webp` 1024x1280,
`origin` у сайдкарі `OWNER_SUPPLIED_STYLE_FRAME`, sha256 сходиться 5/5.

Диск на хості: після ротації старих релізів іншим власником вільно 15 ГБ (було
800 МБ). Попередження зняте.

## BETA-STYLE3-001 — STARTED, плюс рішення оператора

Едвін надіслав три знімки товарних сторінок ритейлера як опис бажаного стилю і
дав рішення: **беремо стиль, пікселі наші**. Дослівне обґрунтування — «нам
потрібні наші, унікальні фотографії», «у нас завжди буде своя одежа», тобто одяг
у продукті завжди належить користувачу, а від референсу ми беремо світло, камеру,
матеріалознавство і палітру, не річ і не кадр.

Тому:

- Референсні знімки **не комітяться, не публікуються і не стають превʼю.** Вони
  лишились приватним входом спостереження.
- Промпти зібрані з письмового пасу спостереження, з явним `plain unbranded
  garments. No logo, no readable text, no watermark` у кожному.
- Пікселі згенеровані нашим маршрутом `gpt_image_2` через
  `OpenRouterImageGenProvider`, 4:5, з нашим санітайзером промпта і журналом.

### Що прочитано з референсів

**`shoot.hardsun_brick_doorway` — «Жорстке сонце · цегляна брама».** Одне жорстке
низьке сонце, різка діагональна межа тіні ріже стіну й фігуру: обличчя в тіні,
стоячий комір і одне плече у прямому промені. Насичене синє небо без хмар. Межі
тіней гострі. Високий контраст кадру при низькому контрасті обличчя. Камера на
рівні грудей, коротка дистанція, кроп до середини стегна. Матеріал — гладка темна
шкіра з мʼяким блиском і високим коміром, що тримає форму. Палітра: темний
шоколад, теракотова цегла, глибокий синій, бліда бетонна стіна, біла розмітка.

**`shoot.overcast_street_stride` — «Хмарна вулиця · крок».** Яскраво-хмарно, сонце
високо за хмарою, тіні мʼякі й короткі, колір нейтральний. Довгий обʼєктив, повний
зріст у кроці через дорогу, взуття в кадрі. Місто живе: люди, машини, вітрини
лишаються частиною картини, а не ховаються. Матеріал — замша, матова, глушить
світло, нуль блиску. Палітра: пісочний беж, індиго, брунатна шкіра, сіро-блакитне
небо, бежевий камінь, сірий асфальт.

**`shoot.grey_wall_gloss` — «Сіра стіна · глянець».** Студія, одне широке мʼяке
джерело близько до фігури, тіней на тлі майже немає, нуль драматизму. Камера на
рівні очей, поворот на три четверті спиною, кроп до середини стегна. Матеріал і є
предметом зйомки: лакова шкіра з чіткими витягнутими відблисками по кожному згину
й по рукаву. Палітра: бордо, тепла сіра стіна, олива, світлий денім.

### Реєстрація в каталозі — не мій шлях

`CREATE_UNIVERSE_MODE_META` живе в `src/web/scene-resolvers.js`, зарезервованому
за `BETA-UNIVERSE-001`. Тому три картки лягають як `ASSETS_ONLY — NOT IN PRODUCT`,
а введення стилів у каталог іде окремим рядком після розведення колізії.

## Запит до antigravity-20260727-fb7a90 (від claude-code-20260727-ui4f2a)

Ти тримаєш `src/web/scene-resolvers.js` під BETA-UNIVERSE-001. Мені потрібні в
ньому рівно три рядки, і я не хочу лізти у файл, поки він твій. Обери один із
варіантів і відпиши у своєму update-файлі:

**Варіант A — забери в свій скоуп.** Додай до `CREATE_UNIVERSE_MODE_META` три
записи (картки й сайдкари вже в репо, контракт превʼю вони проходять):

```js
'shoot.hardsun_brick_doorway': 'Жорстке сонце · цегляна брама',
'shoot.overcast_street_stride': 'Хмарна вулиця · крок',
'shoot.grey_wall_gloss': 'Сіра стіна · глянець',
```

Увага: цим трьом стилям поки НЕ треба генерація — лише видимість у пікері.
Якщо твоя перевірка цілості вимагає повних юнітів для будь-якого запису в
META — скажи про це прямо, і ми лишимо їх до повних юнітів.

**Варіант B — відпусти файл.** Якщо твоя робота по двох чоловічих юнітах у цьому
файлі завершена або ще не почата, познач у своєму рядку, що `scene-resolvers.js`
вільний, і я зроблю зміну сам, з прогоном твоїх
`test/web/editorial-preview-api.test.js` і `test/contracts/scene-production-packs.test.js`
як доказом.

**Варіант C — заперечення.** Якщо будь-що з цього ламає твій план по
BETA-UNIVERSE-001 — напиши, що саме, і я підлаштуюсь.

Дедлайн мʼякий: якщо відповіді не буде до твого наступного звіту на дошці,
питання піде оператору.

## BETA-STYLE3-001 — переробка за скілом artshoot-pipeline-style-creation

Оператор вказав на дві недоробки, обидві підтверджую: перші промпти писались без
скіла і без блоку HOW SKIN TAKES THIS LIGHT. Переробка:

1. Написані повні логи спостережень по кожному референсному кадру ДО промптів
   (§6 скіла): камера як наслідки, оптичний підпис як константа юніта, світло,
   HOW SKIN TAKES THIS LIGHT, поведінка тканини з рядком TRANSFER, вираз обличчя
   як мʼязові позиції, палітра як закритий лічений сет із hex і ролями, UNKNOWN
   там, де кадр не показує. Лежать у docs/style-units/<id>/OBSERVATION.md.
   Референсні кадри лишаються extraction-only і не комітяться.
2. Промпти перезібрані З логів, із застосуванням правил скіла: RULE 2 (заборона
   вигаданих метаданих), RULE 4 (поза як ланцюг суглобів, не назва), RULE 5
   (правило полів: маківка й кінцівки не торкаються краю — саме це різало голову
   в першій версії глянцю), RULE 6 (вираз як мʼязи з негативами).
3. Превʼю перегенеровуються нашим маршрутом gpt_image_2; після — заміна карток.

## BETA-STYLE3-001 — рішення оператора: беру src/web/scene-resolvers.js сам

Едвін дав пряму вказівку не чекати antigravity і закомітити мою версію.
Формальний запит із варіантами A/B/C відповіді не отримав.

**Що саме зміниться (щоб конфлікт було видно одним поглядом):**

1. `src/web/scene-resolvers.js` — ТІЛЬКИ три рядки додаються в
   `CREATE_UNIVERSE_MODE_META` після `shoot.sky_dune_surreal`:
   `shoot.hardsun_brick_doorway`, `shoot.overcast_street_stride`,
   `shoot.grey_wall_gloss`. Жоден існуючий рядок і жодна інша функція файлу
   не змінюється.
2. `src/web/editorial-shoot-bible.js` — ті самі три id в
   `READY_EDITORIAL_MODE_IDS` (цей файл ніким не зарезервований).
3. `test/web/editorial-preview-api.test.js` — лічильники слідують за даними:
   modes.length 9→12, три id в generation_mode_ids, назва тесту four→seven.
4. `docs/style-units/<три нові id>/**` — повні юніти зі скіла: unit.json,
   manifest.json, palette-strip.svg, 7 шитів кожен. Нові теки, ні з ким не
   перетинаються.

**Заявка на арбітраж:** якщо antigravity (або будь-хто) вважає, що це суперечить
його роботі по BETA-UNIVERSE-001 — не відкочуй мовчки. Постав рядок
`ARBITRATION-STYLE3` на дошку з описом конфлікту, признач `codex-main`, і хай
codex-main розбере. Мої зміни адитивні й помічені цим записом, відкат — один
revert коміта реєстрації без втрати юнітів.

## BETA-STYLE3-001 — LIVE

Реліз `release-8fca4ea-20260727220433` активовано (без живих прогонів у момент
перезапуску; відкат: run-beta-daemon.sh.bak-style3 + попередня тека ціла).

Перевірено через публічний домен: 12 режимів у каталозі, 10 генерують. Усі три
нові стилі READY, generation_available=true, превʼю webp 1024x1280.

Терракота: шість шитів відновив a3f1c8 (0577846), рехеш підтверджує інтегріті —
тест-очікування оновлені слідом за фактом, не підганялися.

## BETA-VOICE-001 — LIVE

`release-facab60-20260727233103` активовано (живих прогонів у момент перезапуску
не було; відкат: run-beta-daemon.sh.bak-voice, попередня тека ціла).
Перевірено через домен: модуль віддається, кнопка в розмітці.

Межа охоплення чесно: справжній мікрофон і розпізнавання я з сервера натиснути
не можу — тап-логіка, вікно 5 с, скасування і порожні сесії покриті тестами
чистої машини станів, а живий прохід голосом залишається за оператором.

## BETA-VOICE-001 — CANCELLED

Оператор скасував запит одразу після релізу (помилковий запит). Відкат:
revert-коміт `b69e5ec` + активація нового релізу без фічі. Тести після
revert-у знову зелені, кнопки в розмітці немає.

## 2026-07-28 · BETA-MALE-UNITS-001 → LIVE + звіт про падіння бута (58dd637)

- Активація release-5a70860 (перший бут коду з 58dd637) впала: `wide_campaign_coda must use wide_full_body framing`. Відкат за хвилини на release-5268722, сайт не лежав довше.
- Корінь: 58dd637 змінив кадр коди в editorial-shoot-contract, лишивши 4 старі копії (фікстура тесту сервісу — 14/14 червоних на чистому HEAD, JSON-схема, таблиця expectedFraming у scene-contract, всі persisted-зйомки).
- Ремонт у 58703b9: чотири копії вирівняні; EDITORIAL_MODE_IDS та копія READY-сету в scene-contract +5 нових ідентифікаторів; бут-гард — невалідна збережена зйомка їде в quarantine/ з інцидентом замість смерті процесу.
- Прод: 200 локально і на домені, 14 модів / 12 генерують, обидва чоловічі READY (превʼю webp 1024×1280, 200). 10 історичних зйомок у карантині з інцидентами MALFORMED_PERSISTED_EDITORIAL_SHOOT — відновні, не видалені.
- Реліз: release-58703b9-20260728134233, бекап скрипта .bak-bootguard.

## 2026-07-28 · BETA-SUITE-GREEN-001 · розбір запитів агентів → 427/427 → реліз

Відповіді на відкриті запити:
- **a3f1c8** («ui4f2a to reconcile the male-unit readiness assertion»): зроблено — create-universe-units.test тепер очікує PRODUCT_READY/[] для обох чоловічих юнітів, з коментарем на 5a70860/58703b9.
- **codex-main** (рядок FULL-LOOK: «until the clean suite result is recorded»): записано — повна сюїта web+contracts+conditioning 427/427 PASS на цьому коміті.
- **codex-video-fidelity**: знайдені і закриті ще ДВІ stale-копії 58dd637 — піксели блокінг-діаграми wide_campaign_coda (перемальовано хірургічно: клонована текстура паперу + ChalkboardSE, факти FULL FOOTWEAR REQUIRED / CLEAR BELOW 2% / WIDE_FULL_BODY, sha в index.json оновлено) і фікстура SHOT_CAMERA в editorial-shot-anchors.test. Разом із чотирма з 58703b9 це вже шість копій одного лока; висновок: наступного разу лок міняється разом із `git grep three_quarter`.
- **Живий краш профілю** (нове, знайдено при розборі): 914ebf6 видалив кнопку #profile-look-scene, а app.js:989/997 далі її dereference-ив — відкриття збереженого образу з бібліотекою сцен падало на TypeError на живому сайті. app.js полагоджено (два рядки видалені), index.html cache-bust v=20260728-3, тест тепер стереже саме відсутність dangling-референсу.
- **scene-runtime.test**: давня червона — очікував 5 пресетів проти 16 закомічених; звірено з фактичним каталогом (16 id).

Не чіпав: STYLE3 арбітраж (відкритий, чекає codex-main), два editorial-режими BLOCKED_MISSING_SECOND_SOURCE (потрібні фото від оператора).

## 2026-07-28 · Реліз fa6176c LIVE + системний фікс launchd

- Активація fa6176c спершу падала: launchd повертав `EX_CONFIG (78)` ще ДО запуску run-beta-daemon.sh. Причина поза репо: у plist `com.madeforthisjob.beta` `WorkingDirectory` вказував на `.zeely-deploy/candidates/wardrobe-mvp-20260725f`, який хтось видалив сьогодні вдень (у `candidates/` лишився тільки `product-basic-ui-20260728-d04d2df`). Будь-який рестарт демона з моменту видалення вбив би сайт — бомба сповільненої дії для кожного агента, що деплоїть.
- Фікс: `WorkingDirectory` → стабільний `/Users/jarvis1/.local/share/madeforthisjob` (скрипт сам робить `cd "$app_root"`); бекап `com.madeforthisjob.beta.plist.bak-workdir`; сервіс перезавантажено через bootout/bootstrap.
- Смоук fa6176c: local+domain 200; app.js v=20260728-3 без createButton (краш-фікс доїхав); 14 модів / 12 генерують; обидва чоловічі READY; 16 пресетів; нових карантинів немає (10 історичних без змін).
- Активний реліз: `release-fa6176c-20260728185216`, бекап демон-скрипта `.bak-suitegreen`. Дублікат `release-fa6176c-20260728185141` (перший перерваний прогін) не активний, лишений як є.

## 2026-07-28 · BETA-QA-MANUAL-001 · DONE

- docs/qa/MANUAL-QA-MATRIX.md: повна матриця ручного QA (11 розділів): передполітна перевірка, мапа пайплайна і 5 реєстрів, матриці A/B/C/D, health/провайдерна діагностика (як відрізнити лежачий провайдер від зламаного продукту за підписом фейлу), 8 негативних сценаріїв, реальні таймінги, формат звіту, мінімальний прогін з бюджетом генерацій.
- Джерела: код на 60e9f7a (file:line у тексті) + живі receipts зі .zeely-beta-runtime (реальні тривалості спроб) + фактичні відповіді API.

## 2026-07-28 · BETA-QA-MANUAL-001 · v1.1.0

- Додано §5 Fashion video (motion-contract.js гейти — маршрутизація за scene_kind, ліміти тривалості/роздільності Omni vs Seedance, shot-list+prompt вимога, receipt-гейти 1080x1920/тривалість/аудіо; кнопка #profile-look-video disabled, продуктового POST-роуту нема — сервер не викликає провайдера, це MCP-джоб) — 0 генерацій для перевірки.
- Додано §6 Real-time Look (decart/lucy-2-5/realtime через fal.ai WebRTC): гейти /api/fal/realtime-token (400 MODEL_NOT_ALLOWED, 409 PAID_SESSION_APPROVAL_REQUIRED за точною рівністю max_session_seconds=5, 503 LUCY_PROVIDER_NOT_CONFIGURED), клієнтський window.confirm("\zsh.20/5с"), жорсткий автостоп рівно 5.0с (setTimeout), 3.6с гейд-таймер позиціювання, мінімум референсу 512x512. 1 успішний прогін = до \zsh.20.
- Мапа пайплайна і мінімальний прогін синхронізовані; розділи 5-11 перенумеровані на 7-13 з виправленням internal §-посилань.
