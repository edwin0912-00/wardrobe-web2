Agent ID: claude-code-20260727-557761
Task ID: OPERATOR-DIRECTED (no board row assigned)
Commit tested: d232505
Rationale/decision: Edwin directed a live style-unit session and approved the
result; committed as reference assets only. No product source file is touched.
Result: two male style units added, one existing unit repaired

What landed:

1. `docs/style-units/shoot.shutter_amber_interior/` and
   `docs/style-units/shoot.ochre_stage_tailoring/` — two new units read from
   five supplied male frames, which are committed under each unit's `source/`.
   Each carries four generated sheets (camera & lens, blocking, expression &
   gaze, garment behaviour), a locally rendered `palette-strip.svg`, and
   `unit.json`. Palette hex values are measured from the source pixels, never
   painted or guessed.

2. `unit.json` adds a `compatibility` field. The five existing units were all
   read from female editorial frames: their blocking sheets literally specify
   a raised hip and a hand on the hip crest, and that pose is what made every
   male test frame read wrong. A style unit must declare the presentation it
   was read from so the catalogue can filter instead of guessing from a photo.

3. `docs/style-units/shoot.sky_dune_surreal/sheet-environment-v2.png` — the
   dune unit produced a person cut out onto a backdrop, because the unit
   itself declared veiled overhead-frontal light and no contact evidence. The
   v2 sheet states a sun contract: one hard low sun, cast shadow leaving the
   soles, ripple micro-shadow, warm sand up-bounce, foot displacement. The
   reshoot reads as one photograph. The unit is not yet rebuilt around it.

Measured findings worth keeping:

- Wardrobe drift is caused by reference weight, not by prompt wording. With
  four style sheets attached against one approved look, the dune reshoot
  silently replaced black trousers and black sneakers with brown corduroy and
  white shoes. Naming each approved garment explicitly in the instruction held
  the wardrobe in every later frame. A per-garment lock is the durable fix.
- Brand graphics on a garment are not reproduced identically across frames;
  folds eat parts of the print. Prompt text does not hold it.

weakened_checks: none.
Help request: NONE
Next action: a third street unit is pending its source frames; the walking
frame currently in review is provisional and not committed.
