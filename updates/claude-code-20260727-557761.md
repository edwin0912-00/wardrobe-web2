HANDOFF: POSSIBLE HAND-OFF FOR CODEX / CLAUDE-CODE
Materials: ~/Desktop/ZEELY_REVIEW_20260727 (9 folders + ОГЛЯД.html, incl.
  0_нові_референси/); workflow journals under
  ~/.claude/projects/*/subagents/workflows/<run-id>/journal.jsonl
Uncommitted: none — everything is pushed
Next action: video-service (locked source + surface + mode -> transport -> store
  -> QA), then a wire contract for the UI; do not touch src/web/app.js, it is
  reserved by opencloud
Guide: docs/AGENT_RESUME_HANDOFF_UA.md
Lane: BETA-VIDEO-SEEDANCE-001 (VIDEO.01-04), 27/27 focused tests green, 0 paid calls


Agent ID: claude-code-20260727-557761
Task ID: OPERATOR-DIRECTED (no board row assigned)
Commit tested: 660e08c
Rationale/decision: operator-directed style session plus a provider
investigation; assets and findings are committed, no product source changed.
Result: two male style units landed; image generation restored on beta

## 1. Style units

Two units read from five supplied male frames, committed with their source
frames, four generated sheets each, a locally rendered palette strip, and a
declaration whose hex values are measured from source pixels.

The declaration gains a `compatibility` field. The five earlier units were all
read from female editorial frames, and their blocking sheets specify a raised
hip and a hand on the hip crest, which is what made every male test frame read
wrong. A unit must declare the presentation it was read from; the catalogue
filters on that rather than inferring anything from a user photo.

The dune unit gains a v2 environment sheet stating a sun contract — one hard
low sun, cast shadow leaving the soles, ripple micro-shadow, warm up-bounce,
foot displacement — because the unit as declared produced a person cut out
onto a backdrop. The reshoot reads as one photograph. The unit itself is not
yet rebuilt around the new sheet.

Two measured findings:

- Wardrobe drift is a reference-weight problem, not a wording problem. Four
  style sheets against one approved look silently replaced the approved
  trousers and footwear. Naming each approved garment explicitly held the
  wardrobe in every later frame. A per-garment lock is the durable fix.
- Brand graphics on a garment are not reproduced identically frame to frame;
  folds eat part of the print. Prompt text does not hold it.

## 2. Generation on beta is restored

Verified against the running service, not from memory.

The image provider is now the Higgsfield route rather than the Codex test
route. Beta reports `ready` with generation and editorial generation
available, on the current beta release.

Two failures were found and fixed on the way, both worth knowing:

- The deployment host carries two different builds of the Higgsfield CLI. The
  newer one resolves first on the service search path and fails every call
  with a transport error; the older one works. Authenticating the CLI is not
  enough — the service must resolve the working build.
- The stored third-party evaluator/image key returns HTTP 401 and is dead.
  That transport was the only one serving the delivery aspect natively.

## 3. The remaining provider needs a server-side key

The remaining provider publishes a REST API with an async task pattern whose
shape matches the existing HTTP provider in `src/providers/`. Adding it is a
small, testable change. It cannot be authorised the way the CLI route was:
there is no device-login flow, only a key issued from the account dashboard,
and API access is gated to a higher plan tier. The key is a credential; it is
issued by the operator and stored where the service already keeps its
credentials, never in Git.

## 4. Privacy finding for the orchestrator

Personal and organisational identifiers are present across tracked files —
control ledgers, a users file, preset identifiers and mood-card filenames —
roughly two hundred and seventy occurrences. Preset identifiers cannot be
renamed casually because contracts and hashes bind them, so this needs one
owned task rather than scattered edits. Two properties matter more than the
secret values themselves: whose project this is, and which named credentials
exist. Both are currently inferable from the tree.

History cannot be rewritten here — force-push is forbidden — so redaction
applies forward only, and earlier commits keep whatever they already carry.

weakened_checks: none.
Help request: a key for the remaining provider; an owner for the privacy task.
Next action: a third street style unit is pending its source frames.

## 5. Public explainer page removed (operator approved)

The public site served an explainer page describing the internal transport
chain, provider adapters and exact model slugs, linked from the footer of the
landing page. It was reachable on both the beta and the production hostname.
Removed the page and its two stylesheets, dropped the footer link, and updated
the one test that read the file. `node --test test/web/progress-model.test.js`
passes 12/12 after the change.

Not changed, by operator decision: identifiers inside code and configuration
that carry a personal name. They are bound to contracts, hashes and stored
mood-card filenames, so they need an owned migration rather than an edit. They
remain visible in a client-side script served by both hostnames.

The production hostname still serves the removed page until it is redeployed;
that host is outside this lane.

## 6. Gender/presentation split — verified absent

Checked against the code and the running service, because a task was about to
be planned on the assumption that it exists.

- `src/`, `web/public/`, `config/` and `schemas/` contain zero occurrences of
  a gender or presentation concept for the person. There is no field, no UI
  control, and no filter.
- The live catalogue returns nine styles in one flat list: four legacy
  editorial modes (two READY) and five `shoot.*` units (four READY, terracotta
  correctly blocked on integrity). Nothing narrows that list per look.
- The `compatibility` field added to the two new units is currently declaration
  only; no product code reads it.

Consequence: a woman's style unit is offered for a man's look and vice versa,
which is exactly what produced the wrong poses in the manual test frames.

Minimum for it to work, in order: capture presentation on the look itself at
approval time as a user-confirmed value rather than an inference; carry it in
the look record; add the same field to every style unit declaration; have the
catalogue sort and mark by it, with the full list still reachable. If the
value is unknown, ask; never guess from a photo.

## 7. New row created: BETA-STD-001 (operator-directed)

Added a row for the standard `std.*` backgrounds, owner
`antigravity-20260727-fb7a90`, relayed from the operator. Reserved paths do not
overlap any active code row. Outcome required: each preset reads as a shot
location rather than a backdrop — agreeing light direction, contact shadow,
aerial perspective — proven by one frame per preset on the same approved look,
with no added pixels and no weakened framing lock.

## 8. Scope narrowed by the operator

Magnific as an app provider is cancelled, not deferred. `BETA-PROVIDER-001` is
marked CANCELLED on the board; no provider file was written and no credential
was requested or handled. Generation stays on the route that is already
authenticated on the host.

Scope is beta only. The production hostname is out of scope for this lane,
including the explainer page that is still served there.

## 9. Canon dated and current state recorded (operator-directed)

`spec/ZEELY_CANON_UA.md` is now `1.1.0`, dated 2026-07-27, with an edit-history
section so a rule's revision is visible without `git log`.

`CORE-AVATAR-002` now matches `QA-003` and the shipped code: full length, top
of the head to the soles. This completes an edit started on 2026-07-26 in
`e8a5675`, which moved `QA-003` to full length and left its twin rule
untouched, so the file carried two opposite blocking requirements for a day and
a half. `prompts/avatar.txt` and the `frontal_full_length_composition` gate in
`src/qa/constants.mjs` were already full length throughout.

`ART-003`..`ART-007` are frozen, recorded in the canon itself with the reason:
they describe a free-standing showcase film, while `docs/VIDEO_LIVE_CANON_UA.md`
— written today — requires every motion to start from an approved master look
and a chosen style unit after `ART_SHOOT.05`. Two incompatible video concepts
are not funded in parallel. `ART-001` and `ART-002` stay in force.
`node tools/validate-canon.mjs` → PASS, 43 rules, 34 blocking, 0 failures.

`docs/CURRENT_STATE_2026-07-27_UA.md` records the last 24 hours across all six
agents, what is LIVE versus CORE_ONLY versus NOT_DELIVERED, and the four things
currently broken, with what each other agent and the operator has to decide.

Also committed: regenerated `output/*/qa-report.json` and `output/qa-summary.json`.
They were rewritten by `node tools/verify-output.mjs` during this session's
audit. Two things changed and both are truthful: the recorded status moves from
PASS to NEEDS_REVIEW because the six artifacts still carry a pre-rename
half-body review under a gate that is now full length, and the reports stop
embedding an absolute path from another machine. Reverting is one checkout.

weakened_checks: none.

## 10. Motion entry point corrected by the operator

`docs/VIDEO_LIVE_CANON_UA.md` required both Video and Live to start only after
`ART_SHOOT.05`, with an approved frame or contact sheet in hand. The operator
ruled otherwise today: the entry point is a **selected look**. As soon as one
master look exists and the user opens it, three equal branches are offered —
Live try-on with the camera, Photoshoot, and Fashion video. An approved
photoshoot is no longer a precondition for motion; it is one of the three exits
from a look. Block order may still change; the entry point is what is fixed.

Amended in place with the previous wording preserved as history, because other
files reference that paragraph. The file was reserved by no active row.
This directly affects `BETA-POSTSHOOT-RECON-001`, whose outcome still reads
"approved fashion shoot → Video or Live"; its owner needs to restate it.

weakened_checks: none.

## 12. UI journey canon written, and five of my own claims withdrawn

`docs/UI_JOURNEY_CANON_UA.md` v1.0.0 — twenty-two ruled `UIJ-*` items for the UI
journey only; it overrides nothing in `spec/` or in the motion canon. Every rule
carries its proof, MUST is reserved for real defects, and the file ends with a
dated edit history.

Two operator rulings are now recorded as rules rather than living in chat:

- `UIJ-021` — the motion entry point is a selected look, with three equal
  branches out of it.
- `UIJ-022` — a background-free avatar is an authorized UI-only display asset.
  The alpha refusals in `src/qa/white-normalizer.mjs` and
  `src/qa/image-inspector.mjs` guard artifacts that enter generation and QA; they
  say nothing about a derived display asset. My earlier claim that "the canon
  forbids it" was wrong and is corrected in the rule text. Note for whoever picks
  this up: `web/public/chroma-key-extractor.js` already exists (keys `#00ff00`),
  is imported by `index.html` and `lusion-master.html`, and is called by nothing.

Withdrawn from my 2026-07-28 scheme, listed in the canon so the retraction is
readable without this file:

1. "Remove the five confirm dialogs" — presented as a client-only change, but
   `deleteLook` hard-deletes at `profile-service.js:1013` and the routes flush
   the deletion queue immediately (`:1357-1358`), so there is no undo to replace
   it with. Confirms stay until soft-delete exists.
2. "One primary action per state" versus the flat three-card fork — my own
   contradiction. The rule is rewritten: several equal exits are allowed; a
   second competing forward action beside them is not.
3. "Remove the header MVP link" — already absent; the item came from a stale
   snapshot.
4. "Five `window.confirm`" — there are four in `web/public/*.js`.
5. "The canon forbids a background-free avatar" — false, per above.

Also recorded: nine places where the canon and the shipped product disagree,
sized as copy-only, client-and-server, or needs-a-decision, plus three open
decisions for the operator (how the transparent avatar gets its key, the
completeness bar for Live, and whether soft-delete is wanted).

Not committed here: the `live-look-reference` work from the previous step stays
uncommitted while its framing is settled.

weakened_checks: none.

## 13. Live look reference committed with the corrected framing

Committing the work from step 11 under the description it should always have had.
It is **not** a transparent avatar. It builds the garment-only reference card the
Live mirror needs: the approved look's already hash-verified item cutouts are
composited onto one exact-white 1024x1024 card and served by look id together
with the card's own sha256. The person is deliberately absent — on the mirror the
person arrives on the camera track, so a person in the reference is a second
competing identity, and the mirror's prompt already assumes a garment card.

Reuses the existing lock verbatim (`approvedLookReference` → `resolveApprovedLook`
→ `approvedItemEvidenceForRun`), adds no new authorization idiom, invents no
pixels, re-checks each cutout hash itself, and refuses an incomplete look rather
than padding it out. `node --test test/live/` → 15/15.

Client and paid-token wiring stay a handoff to `codex-live-20260727`, whose files
this does not touch.

weakened_checks: none.
