Agent ID: claude-code-20260727-a3f1c8
Protocol ACK: 036b20a
Task ID: BETA-VIDEO-FIDELITY-001 — evidence report only. No product code touched, no provider work run under this row, and the core item contract deliberately left alone.
Commit tested: 036b20a (board); the runtime evidence below was gathered on live release abd9afd
Rationale/decision: The instruction was to report only what is needed to reserve a narrow full-look INPUT gate, so this report is scoped to input completeness and says nothing about what the item contract should become. The distinction matters: the defect is reachable without changing the contract at all, because it is a property of what the refpack resolves rather than of what the contract declares.
Result: Two fifteen-second runs, same avatar, same target style, differing only in refpack composition. RUN A refpack: cleaned reference video plus one delivered scene frame that carried its own environment plus four cut-outs on white. Outcome: footwear correct in every visible shot, but the location split — roughly half the shots relocated to a garden with a swimming pool that appears in no prompt and in no video reference. The single environmental image was the only environment source in the pack. RUN B refpack: identical except every person-role reference was matted to flat white (the delivered frame background-removed, plus a tight face crop background-removed) and no environmental image was included. Outcome, sampled every 0.5s across all fifteen seconds, thirty frames: one consistent location throughout, our own black ballet flats in every shot where footwear is visible, navy jeans to the ankle in every such shot, and no foreign limb anywhere. The earlier defect, for the record, was one shot at 9.5s carrying a bare leg in a strappy stiletto sandal at the same timecode as the reference video's own footwear close-up. Two measured input properties, and nothing else, separate the failing run from the passing one: (1) no reference whose role is person, body, garment or expression may contain an environment; (2) every garment category a close shot can reveal must be present in the pack at close-shot scale — the avatar's shoes occupied roughly 150 soft pixels in a full-length frame while the reference showed a stranger's sandal at roughly 700 sharp pixels. For context only, not as a proposal: the approved-items evidence for this look contains exactly one entry, category `top`, and the blocking sheet states `FULL FOOTWEAR: NOT REQUIRED`.
Evidence command: node -e "console.log(JSON.parse(require('fs').readFileSync('<runtime>/scenes/<scene>/inputs/approved-items/evidence.json')).items.map(i=>i.category))"
Help request: whether the reserved gate should be an input-completeness precondition on the refpack, which needs no contract change, or whether the owner of the item contract wants it expressed there instead. This agent will not choose that.
Next action: none. Awaiting the reserved row before any further work, and running no further provider generations under this finding.
