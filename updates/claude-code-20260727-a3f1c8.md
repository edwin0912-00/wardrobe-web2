Agent ID: claude-code-20260727-a3f1c8
Task ID: none — QA finding on the video block, reported before any code is proposed.
Commit tested: ac7259b
Rationale/decision: A reference video was supplied for the video MVP and the operator's question was whether a complex clip can be copied with our own avatar and outfit, consistently and repeatably. Measuring that surfaced a defect whose root is in the still pipeline, not in the video work, so it is reported here rather than fixed quietly.
Result: Copying works. With the reference as a video reference and the approved look as image references, the location, the cut rhythm, the shot vocabulary and the energy transfer, and the identity holds. Two identical runs showed that identity, garment, location, palette and grade are repeatable while the exact edit is not — Seedance exposes no seed, so bit-identical output is impossible by construction. The defect: in one shot of the fifteen-second run the subject's lower body was copied wholesale from the reference — a bare leg in a strappy stiletto sandal, at the same timecode as the reference's own footwear close-up. The cause is that `approved-items` for this look contains exactly one item, the hooded sweatshirt, with a full forensic dossier; the jeans and the footwear are locked nowhere, and the blocking sheet states `FULL FOOTWEAR: NOT REQUIRED`. The sweatshirt did not drift in a single frame of three clips, so the locking mechanism works — it was simply never applied below the waist. ITEM_FIDELITY cannot fail what nothing declared.
Evidence command: node -e "console.log(require('<runtime>/scenes/<scene>/inputs/approved-items/evidence.json').items.map(i=>i.category))"
Help request: codex-main to decide whether footwear and bottoms should become first-class approved items with their own dossiers, since that is a change to the item contract and not something this agent should choose alone.
Next action: a corrected generation is running with dedicated footwear, hem and sleeve detail references cut from the approved look, and with the reference's own footwear close-ups removed from the video reference; the result will be reported here.
