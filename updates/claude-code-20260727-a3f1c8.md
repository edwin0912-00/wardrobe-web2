Agent ID: claude-code-20260727-a3f1c8
Protocol ACK: 036b20a
Task ID: BETA-SKILL-RULE9-001 — corrected and DONE
Commit tested: documentation only; the investigation below touched no product code
Rationale/decision: RULE 9 as first committed asserted that the product repeats one framing across its six slots. That was checked afterwards and is false, so the rule is corrected rather than left standing — a skill carrying a wrong claim about the pipeline is worse than no rule.
Result: The product does NOT have the defect. Each slot's prompt already carries a distinct focal length (50/50/65/55/85/35), a distinct camera height, its own angle sentence, its own pose sentence, and the subject-height band read from that slot's own lock — assembled at `editorial-shoot-bible.js` around the CAMERA and POSE lines. Only the crop token is shared across five of six slots, and the comment at the site documents why: `full_length` forced the generator to invent a lower garment and footwear the approved look never contained, ITEM_FIDELITY correctly refused to verify invented items, and the first slot became unpassable and blocked the three crop slots behind it. So that flattening is a reasoned workaround for a real gate conflict. Both defects the operator caught were in a hand-authored run outside the product, not in the pipeline: the repeated framing, and a detail frame containing no person — which the pipeline also already guards, its detail slot asking for "anatomically plausible hand or body context". No product change was made, because the correct change here is no change. The one change that would unlock genuinely varied coverage — a coda that is actually full length — is gated on footwear becoming a locked item, which is `BETA-VIDEO-FIDELITY-001`, still BLOCKED awaiting the contract decision.
Evidence command: sed -n '106,190p' src/web/editorial-shoot-bible.js
Help request: NONE. The dependency is already recorded on BETA-VIDEO-FIDELITY-001.
Next action: none on this row. Coverage variety in the product is downstream of the footwear item decision, not of the bible.
