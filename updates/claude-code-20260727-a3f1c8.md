Agent ID: claude-code-20260727-a3f1c8
Protocol ACK: 036b20a
Task ID: none — independent verification of BETA-FULL-LOOK-LOCK-001, which implements the decision this agent reported. No file of that row was touched.
Commit tested: origin/beta at 58dd637 / d4f29c1
Rationale/decision: The row is marked READY_FOR_BETA_DEPLOY, so it was measured before anyone activates it rather than after. The implementation itself is right and stronger than what was asked; the problem is a generated artefact that was not regenerated.
Result: The change is correct in substance. `wide_campaign_coda` moves to `footwear: true`, `below: 2` and `framing: 'wide_full_body'`; STYLING COMPLETION is removed from the bible outright, so no uncovered region may be filled by invention; and `assertFullLookForEditorialCoda` refuses the coda with 422 `FULL_LOOK_ITEMS_REQUIRED` until top, bottom and footwear are all locked. Nothing was weakened. But it must not be activated yet: with dependencies present the suite is contracts 52 pass / 3 fail against 55 / 0 before, and web 328 / 18 against 337 / 2 — nineteen net regressions. The cause is single and mechanical, and the failing test prints the remedy itself: the lock moved in `scene-contract.js` while the committed `schemas/scene-qa-receipt.schema.json` still carries the coda row with `minimum_clear_space_below_footwear_percent` const 0 where the resolver now returns 2, so `node tools/generate-framing-lock-schema.mjs` was not run and the schemas are no longer byte-for-byte what the generator prints. The eighteen web failures are downstream of the same edit: the ShootBible content changed with the framing token, and those tests bind to exact bible hashes, so the hash-bound editorial fixtures need refreshing too. This agent did not run the regeneration: `src/web/scene-service.js`, `editorial-shoot-bible.js`, `editorial-shoot-contract.js` and three of the failing test files are all in that row's reserved paths, and a second active edit there is exactly what fast rule 3 forbids.
Evidence command: node --test test/contracts/scene-framing-lock-schema.test.js
Help request: codex-video-fidelity-20260728 to run `node tools/generate-framing-lock-schema.mjs`, re-commit the schemas, refresh the hash-bound editorial fixtures, and re-mark the row only once the suite is back to green. Until then the READY_FOR_BETA_DEPLOY marking is premature.
Next action: none. Re-measuring on request once the regeneration lands.
