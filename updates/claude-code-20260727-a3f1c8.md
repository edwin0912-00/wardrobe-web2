Agent ID: claude-code-20260727-a3f1c8
Protocol ACK: 036b20a
Task ID: BETA-VIDEO-FIDELITY-001 — the operator decision this row was blocked on
Commit tested: n/a; this commit carries a decision, not code
Rationale/decision: The row was BLOCKED pending a decision on whether footwear and bottoms become first-class approved items or whether the gate stays an input-completeness precondition. Edwin decided today. Recording it verbatim in effect so the owner can reserve the fix without another round trip, and deliberately not implementing it here, since this row forbids repairing the item contract unassigned.
Result: The decision is that an item which will be VISIBLE in delivery must be a locked item. Its exact shape, and it is broader than footwear: the lock is about consistency, not about provenance. Where the approved look does not contain the item, it may be synthesised once — but from that moment it is a declared item with its own dossier and hash, and every later frame must match it. Inventing it afresh per frame is what is forbidden; inventing it once and freezing it is allowed. Where the item does exist in the approved look, its detail must come from a real crop of it and never from a prettier synthesis. Practical consequence for this repo: `approved-items` currently holds exactly one entry, category `top`; jeans and footwear are locked nowhere and the blocking sheet states `FULL FOOTWEAR: NOT REQUIRED`. That is the knot behind two separate symptoms already reported — a video generation borrowing a stranger's sandal, and `wide_campaign_coda` being unable to be genuinely full length because `full_length` forces an invented lower half that ITEM_FIDELITY correctly refuses. Locking footwear and bottoms unties both. Each locked item needs what the hoodie already has: a product view, a forensic dossier of colours, construction, material and pattern, and a sha256 — no half measures.
Evidence command: node -e "console.log(JSON.parse(require('fs').readFileSync('<runtime>/scenes/<scene>/inputs/approved-items/evidence.json')).items.map(i=>i.category))"
Help request: codex-main to reserve the contract/gate change now that the decision exists. This agent will not start it unassigned.
Next action: none on this row until it is reserved.
