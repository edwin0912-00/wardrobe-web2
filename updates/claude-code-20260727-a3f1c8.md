Agent ID: claude-code-20260727-a3f1c8
Protocol ACK: 036b20a
Task ID: BETA-SKILL-RULE9-001 — DONE in this commit
Commit tested: no code; additive documentation under this row's reserved paths
Rationale/decision: The operator caught two defects in generated coverage that no gate caught, so they are written into the skill rather than remembered. Both came from generating a shoot without reading what the product's coverage contract actually requires.
Result: RULE 9 added. First defect: six styles were generated and every frame was the same composition — full length, centred, camera at chest height — because the delivery lock was written into every prompt and flattened the camera character out of six shoots that each had their own. Second defect: the detail frame contained no human at all. The slot declares `head: false`, which means the head need not be visible, not that the person is absent; a crop of cloth is a product shot and this product sells a person wearing the item. The contract does not catch that, because the detail slot requires no body at all, so a flat lay passes the gate — recorded in the rule as a known gap for the contract owner. The rule also records the six real locks read from `editorialFramingLock` rather than assumed, and three consequences that each bit: there is no tight face close-up slot, because five slots need the head visible with the subject between 40 and 96 per cent and a collarbone-up crop is effectively 100; `wide_campaign_coda` bottoms out at 30 per cent so a tiny figure has a floor; and footwear is `false` in every one of the six, which is precisely how a video generation borrowed a stranger's sandal.
Evidence command: git diff --numstat HEAD~1 -- skills/artshoot-pipeline-style-creation/SKILL.md
Help request: the contract owner to decide whether the detail slot should require a visible body part, since a flat lay currently passes.
Next action: regenerate the remaining five portfolio styles as proper six-slot coverage rather than one framing repeated.
