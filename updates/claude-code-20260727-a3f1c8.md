Agent ID: claude-code-20260727-a3f1c8
Protocol ACK: 036b20a
Task ID: BETA-VIDEO-STAGE-001 — contract layer landed
Commit tested: this commit; contracts suite in full
Rationale/decision: The post-shoot graph declares mode `video` with nodes VIDEO.01–04 and a contract test pins the mode list, while `live_webcam` is fully implemented and `video` has no executor at all — `grep VIDEO.0` over src/ returns nothing. This is the first layer of that executor: the contract, so nothing downstream can be built on assumptions. The 23.07 branch was deliberately not merged; it is 353 commits behind and its diff deletes 64k lines.
Result: `schemas/motion-job.schema.json`, `src/web/motion-contract.js` and 12 tests, all green. Every rule in it is a measurement from 2026-07-27 with the failure it prevents written next to it, so nobody re-derives them by spending credits: the route is decided by scene kind and cannot be overridden — ordinary backgrounds to Omni, art and fashion to Seedance; each model is held to its own ceilings, Omni at 10s/720p with a 3-second reference clip and Seedance at 15s/1080p; a shot list is refused on Seedance because it produces its own order; a person, garment or footwear reference must be a cut-out on flat white; a reference clip must have its own footwear close-ups cut out; identity and footwear detail are both mandatory in the pack; `character` is deliberately not a role because a real face in that slot is refused by moderation every time; delivery is 9:16 only and transport is `mcp` only; and the model's invented audio can never ship. Writing the tests caught a wrong fixture of mine rather than a wrong rule — I had handed Omni a five-second reference clip, which is a Seedance figure.
Evidence command: node --test test/contracts/motion-contract.test.js
Help request: NONE
Next action: the service layer — emit a job for a look, verify a returned receipt, and wire the four VIDEO nodes to it. The provider boundary stays at the job file because the server cannot hold an MCP session.
