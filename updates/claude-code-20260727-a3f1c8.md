Agent ID: claude-code-20260727-a3f1c8
Protocol ACK: 036b20a
Task ID: none — correction to this agent's own previous survey, plus the terracotta integrity cause.
Commit tested: live release abd9afd
Rationale/decision: The previous report in this file stated that no ART_SHOOT artefact exists. That was wrong and is corrected here rather than left standing, because other agents read this file. The error was reading a directory named `shoots` when the runtime uses `editorial-shoots`.
Result: CORRECTION — ten editorial shoots exist on the beta runtime, not zero: two COMPLETED, five NEEDS_RETRY, three HERO_PENDING_APPROVAL, each with six shots. Classified by `bindings.shoot_bible.mode_id`, all ten belong to the old `editorial.*` family — `organic_contrast` with 2 COMPLETED, 4 NEEDS_RETRY, 1 pending, and `urban_monochrome` with 1 NEEDS_RETRY, 2 pending. The part of the earlier claim that survives, now verified through the correct field: none of the five new `shoot.*` art-fashion styles has ever been run, so the style-unit work has never been exercised end to end. An approved look is available and is the one all ten shoots used. Separately, the cause of `shoot.terracotta_hardlight` BLOCKED_INTEGRITY_MISMATCH is found and it is this agent's own defect, not tampering: `docs/style-units/shoot.terracotta_hardlight/manifest.json` declares the sha256 of the original sheets, while the committed PNGs are recompressed copies — 20 MB in the repo against 47 MB of originals, six of seven hashes differing. `git log` shows those files were added in one commit by this agent and never modified since, and the original bytes still exist off-repo and match every declared hash exactly. So the unit was internally inconsistent from the moment it was committed. Two ways out, and the difference is not technical: commit the original bytes the manifest already declares, which rewrites no recorded fact and costs 27 MB; or recompute the manifest against the tree, which keeps the size and is exactly the retroactive legalisation of byte changes that `docs/CURRENT_STATE_2026-07-27_UA.md` warns against. This agent recommends the first and will not choose it alone. The other two blocked modes fail on BLOCKED_MISSING_SECOND_SOURCE, an unrelated cause that no hash change can fix.
Evidence command: node -e "const {createHash}=require('crypto'),fs=require('fs');const D='docs/style-units/shoot.terracotta_hardlight';for(const s of require('./'+D+'/manifest.json').sheets)console.log(s.file, createHash('sha256').update(fs.readFileSync(D+'/'+s.file)).digest('hex')===s.sha256)"
Help request: a decision on which terracotta route to take, since one of them costs 27 MB and the other alters a recorded hash. Until then terracotta stays blocked and only four of the five art-fashion styles can be exercised.
Next action: none without a row. No provider generation started for the art-fashion styles.
