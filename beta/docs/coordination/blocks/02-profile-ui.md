# Chat 2 — Beta Block 2: profile and pipeline UI

Owner: `chat-2`.
Branch: `beta-block-2-profile-ui`.
Pipeline: user-facing `PROFILE`, `LOOK`, `CHOICE`, pipeline explainer.

Own browser drafts, upload-field persistence, saved avatar/look navigation,
Add clothing for one existing avatar, duplicate choice UI, real backend
progress/nodes, result actions and engineering explanation. Do not change
generation, persistence backend or QA.

First atom: manually prove refresh recovery and saved-avatar → Add clothing →
result actions on current beta, then fix only the first reproducible UI failure.
