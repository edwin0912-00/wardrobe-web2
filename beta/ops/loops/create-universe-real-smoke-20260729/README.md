# Create Universe real smoke

Run only from the beta worktree. This is a bounded provider proof, not a bulk
generation job: one standard background, one `shoot.*` hero, then at most one
hero for each remaining ready Create Universe style after both canaries pass.

The loop never uses user uploads, commits images, prints prompts, exposes
credentials, or treats a provider response as a pass without the typed QA receipt.
