# `a/` is the archived, superseded version-A build — not part of the current site.
a/

# Local safety copies made before surgical edits (safe-artifact-stewardship). Not history.
*.bak
*.bak-*
*.bak-precanon

# Deployment credentials and runtime backups never belong in an installable
# or evaluator-facing branch, even when encrypted.
beta/secrets/*.enc
beta/secrets/*.tar.gz
beta/secrets/*.json
beta/secrets/*.pem
beta/secrets/*.key

node_modules/
beta/node_modules/
artifacts/self-check/
test-results/
playwright-report/
.wardrobe-media-bundle.json

# Materialized from the SHA-locked GitHub Release bundle during installation.
/b/assets/
/b/audio/
/beta/assets/
/beta/docs/style-units/
/beta/output/
/beta/inputs/
/beta/artifacts/
